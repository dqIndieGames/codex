# local1 清单遗漏核对_总控记录_2026-04-27

## 任务目标

- 根据 `docs/local1-custom-feature-checklist-2026-03-28.md` 核对当前还缺哪些文档项、证据项或口径收敛项。
- 按 `$codexflow-temp-output-writer` 输出规则，把正式产物写入当前项目 `.codexflow/临时/<主题>_<日期>/` 下的新目录。
- 本轮是文档任务，不修改代码、不格式化、不执行构建或测试。

## 输入与边界

| 类型 | 路径或来源 | 用途 |
|---|---|---|
| 主输入 | `docs/local1-custom-feature-checklist-2026-03-28.md` | 判断当前 local1 长期定制清单已有内容与遗漏 |
| 当前版本线索 | `codex-rs/Cargo.toml:104` | 确认当前仓库版本已是 `0.124.0` |
| 当前发布文档 | `docs/local1-release-rust-v0.124.0-2026-04-24.md` | 对照 0.124.0 local1 发布说明 |
| 当前代码静态线索 | `codex-rs/core/src/stream_events_utils.rs`、`codex-rs/core/src/session/*`、`codex-rs/app-server/src/*`、`scripts/windows_app_server_refresh_tray.py` | 判断源清单里的代码迹象是否过期或缺项 |
| 当前测试线索 | `codex-rs/app-server/tests/suite/v2/thread_provider_runtime_refresh.rs`、`codex-rs/core/src/stream_events_utils_tests.rs` | 判断是否缺少可机械执行的验证矩阵 |

边界：

- 未联网核对 OpenAI/Codex upstream 最新 release；本轮只使用本地仓库静态内容。
- 未执行 `cargo test`、`just fmt`、`just fix`、构建、lint。
- 未修改 `docs/local1-custom-feature-checklist-2026-03-28.md` 或任何源码。

## 流程发现

### 目标

输出一份“遗漏清单文档”，让后续修正文档的人能逐项处理，不需要重新从聊天记录推断。

### 入口

- 文档入口：`docs/local1-custom-feature-checklist-2026-03-28.md`
- 当前 release 入口：`docs/local1-release-rust-v0.124.0-2026-04-24.md`
- 版本入口：`codex-rs/Cargo.toml`
- A1 首轮清单入口：`codex-rs/core/src/stream_events_utils.rs`
- Provider refresh 入口：`scripts/windows_app_server_refresh_tray.py`、`codex-rs/app-server/src/windows_control.rs`、`codex-rs/app-server/src/codex_message_processor.rs`
- Session/retry 入口：`codex-rs/core/src/session/turn.rs`、`codex-rs/core/src/session/session.rs`、`codex-rs/core/src/client.rs`

### 分支

| 分支 | 处理 |
|---|---|
| 文档内部缺口 | 写入遗漏清单，标为主文档结构或口径遗漏 |
| 文档与当前仓库静态线索不一致 | 写入遗漏清单，标为代码迹象或发布状态遗漏 |
| 需要执行测试才能确认 | 不伪装成已验证，写为“缺少验证矩阵/未执行验证” |
| 需要修改代码才能收敛 | 只列后续动作，不在本轮执行 |

### 依赖

- PowerShell 显式 UTF-8 读取中文文档，避免乱码判断。
- `rg` 静态检索当前仓库相关路径。
- `$codexflow-temp-output-writer`：正式产物必须写入 `.codexflow/临时` 新目录。
- `flow-driven-task-execution`：本轮按流程发现、todo、总控记录、执行与验收收口。

### 验收

- 产物目录位于 `.codexflow/临时/local1清单遗漏核对_2026-04-27/`。
- 至少输出一份遗漏清单文档。
- 文档明确区分“确认遗漏”“需要用户后续决定”“本轮未验证”。
- 不修改代码，不修改源清单。
- 写入后回读，确认 UTF-8 无 BOM 且无替代字符。

### 用户影响

最终使用这份清单的人会直接看到：当前 `local1` 基线文档还有哪些地方容易导致后续同步、复核或 release 说明跑偏；哪些只是文档补充，哪些如果要彻底收敛会需要后续代码或测试任务。

## Todo 总表

| 编号 | 动作 | 输入 | 输出 | 通过条件 | 状态 |
|---|---|---|---|---|---|
| T1 | 读取输出规则与源文档 | skill 文件、源清单 | 已确认输出目录和源文档结构 | 中文正常读取，无乱码 | completed |
| T2 | 发现流程与证据入口 | 源清单、release 文档、代码静态检索 | 证据入口表 | 能区分文档、代码、验证三类缺口 | completed |
| T3 | 静态核对遗漏点 | 源清单和本地仓库静态线索 | 遗漏项分级 | 每条遗漏有证据、影响和建议动作 | completed |
| T4 | 写入正式产物 | 遗漏项和总控记录 | 总控记录、遗漏清单 | 文件写入 `.codexflow/临时` 新目录 | completed |
| T5 | 回读与收口 | 新产物 | 编码检查和最终说明 | UTF-8 无 BOM、无替代字符；结论说明未修改代码 | completed |

## 执行记录

- 2026-04-27：读取 `$codexflow-temp-output-writer`，确认正式产物必须写入当前项目 `.codexflow/临时/<自定目录>/`。
- 2026-04-27：用显式 UTF-8 方式读取源清单，确认其结构为 `121 同步基线`、`当前推断范围`、`2026-04-17 同步判定矩阵`、`定制功能主清单`、`同步官方后的必查清单`、`2026-04-10 归档补充`。
- 2026-04-27：静态核对当前仓库，确认 `codex-rs/Cargo.toml:104` 当前版本为 `0.124.0`，而源清单仍以 `rust-v0.121.0` 为同步基线。
- 2026-04-27：静态核对 `codex-rs/core/src/stream_events_utils.rs:44`，发现 A1 首轮清单可见文案仍包含“继续保留 provider 身份信息”，与源清单 `F11` 当前去 provenance 口径存在冲突风险。
- 2026-04-27：静态检索 `codex-rs/core/src/session/*`、`codex-rs/app-server/src/*`、`scripts/windows_app_server_refresh_tray.py`，发现源清单部分“当前代码迹象”没有覆盖当前主要承载文件。
- 2026-04-27：回读两个新建 Markdown 文件，结果为 UTF-8 无 BOM、无替代字符、无问号替代字符。

## 证据与文件

| 证据 | 结论 |
|---|---|
| `docs/local1-custom-feature-checklist-2026-03-28.md:7-20` | 源清单仍以 `rust-v0.121.0` 为本轮同步基线，并排除后续 `main` 变化 |
| `codex-rs/Cargo.toml:104` | 当前仓库版本为 `0.124.0` |
| `docs/local1-release-rust-v0.124.0-2026-04-24.md:5-18`、`:22-35` | 本地已有 0.124.0 local1 发布说明 |
| `docs/local1-custom-feature-checklist-2026-03-28.md:55-56`、`:127-134` | A1/A2 已出现在矩阵和归档补充中 |
| `docs/local1-custom-feature-checklist-2026-03-28.md:58-76` | 主清单只有 F1-F15，未把 A1/A2 纳入同等主表 |
| `docs/local1-custom-feature-checklist-2026-03-28.md:72`、`:113` | F11 明确不再把 `thread/list` provider provenance 保真作为通过条件 |
| `codex-rs/core/src/stream_events_utils.rs:44` | 首轮清单当前仍写“继续保留 provider 身份信息” |
| `codex-rs/core/src/session/turn.rs:1076`、`:1870`、`:2055`、`:2220` | 当前 session/retry/A1 逻辑已在 session 模块中有活动入口 |
| `codex-rs/app-server/tests/suite/v2/thread_provider_runtime_refresh.rs:171`、`:198`、`:254`、`:289`、`:317`、`:373`、`:432` | 当前已有 provider refresh 关键测试线索，但源清单没有形成推荐验证矩阵 |

## 阻塞与用户决策

- 无阻塞项影响本轮遗漏清单产出。
- 需要用户后续决定的事项：是否把 A1 提升为 `F16`，或新增“归档项主清单”；是否把中央清单升级为 0.124 当前基线，还是保留 121 为历史章节并另建当前状态章节。

## 验收结果

- 已按要求只做文档产出，不修改代码。
- 已创建 `.codexflow/临时/local1清单遗漏核对_2026-04-27/`。
- 已输出遗漏清单文档。
- 已完成编码回读：两个新建 Markdown 文件均为 UTF-8 无 BOM，未发现替代字符或问号替代字符。

## 最终结论

本轮已完成 `local1` 基线清单的遗漏核对，主要缺口集中在当前 0.124 同步状态未回写、A1/A2 未纳入主清单、F11 与首轮可见文案存在 provider 身份口径冲突、当前代码迹象和验证矩阵不够完整；本轮未修改任何代码或源清单。
