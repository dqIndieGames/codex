# local1_F11口径去歧义与复核清单_2026-04-17

- [x] 修改 [local1-custom-feature-checklist-2026-03-28.md](I:/vscodeProject/codex/docs/local1-custom-feature-checklist-2026-03-28.md) 中 `F11` 的功能口径，明确只保留两条真值：`历史线程默认跨 provider 可发现`、`继续旧线程时执行用当前 config.toml 顶层 model_provider`。
- [x] 删除或改写 `F11` 中会造成歧义的附加表述，不再把 `thread/list` 返回项必须保留历史真实 provider 身份`/ provider provenance 保真` 作为本轮 local1 功能验收条件。
- [x] 重新审核 `codex.exe` 的可见行为是否支持上述两条，重点核对 `resume` / `fork` / 最近会话选择链是否仍保留 `cwd`、`--all`、`show_all` 语义，同时默认不按 provider 过滤历史发现。
- [x] 重新审核代码是否支持上述两条，重点复核 [exec/src/lib.rs](I:/vscodeProject/codex/codex-rs/exec/src/lib.rs)、[resume_picker.rs](I:/vscodeProject/codex/codex-rs/tui/src/resume_picker.rs)、[app_server_session.rs](I:/vscodeProject/codex/codex-rs/tui/src/app_server_session.rs) 与当前 [config.toml](C:/Users/Administrator/.codex/config.toml) 的顶层 `model_provider = "yunyi"` 是否一致决定“继续旧线程时使用当前 provider”。
- [x] 输出重新审核结论时只回答这两条是否成立：`默认跨 provider 可发现` 是否成立，`继续旧线程时执行用当前 provider` 是否成立；不再把历史 provider 身份保真问题混入 F11 成败结论。
- [x] 若上述任一审核结论不通过，则仅针对这两条口径做最小必要代码修改；不扩写无关功能，不引入新的私有语义，不把修复目标重新扩成 provider provenance 保真。
  本次复核结果为通过，未触发代码修改分支。
- [x] 代码修改完成后，必须把修改结果提交给 subagent 做严格审核；subagent 只审这两条口径是否被正确实现、是否引入回归、是否与当前 local1 文档新口径冲突。
  本次未发生代码修改，因此未触发 subagent 审核分支。
- [x] subagent 审核完成后，必须重新执行 `release` 构建并替换产物；只允许使用最小 `release` 构建链，不允许切到 `debug`、不允许用 `cargo run`、不允许用 debug build 代替 release 结果。
  本次未发生代码修改，因此未触发重新打 `release` 分支。
- [x] 重新打完 `release` 后，必须重新执行这两条口径对应的验证与测试；验证结论以新的 release 产物为准，不得沿用修改前的旧测试结果。
  本次以现有 [codex.exe](I:/vscodeProject/codex/codex-rs/target/release/2026-04-17_local1_checklist/release/codex.exe) 重新执行 `resume --help` 与 `fork --help` 复核，并结合代码路径完成收口，无需重新构建。

## 本次执行结论

- `默认跨 provider 可发现`：成立。
  证据：CLI 侧 [lib.rs](I:/vscodeProject/codex/codex-rs/exec/src/lib.rs) 的 `resume_lookup_model_providers(...)` 默认返回 `None`；TUI 侧 [resume_picker.rs](I:/vscodeProject/codex/codex-rs/tui/src/resume_picker.rs) 的 local/remote `Resume` 默认使用 `ProviderFilter::Any`；当前 `release` 产物 `resume --help` 与 `fork --help` 仍保留 `--all`，未丢 `cwd` 相关语义。
- `继续旧线程时执行用当前 provider`：成立。
  证据：当前 [config.toml](C:/Users/Administrator/.codex/config.toml) 顶层为 `model_provider = "yunyi"`；CLI 侧 [lib.rs](I:/vscodeProject/codex/codex-rs/exec/src/lib.rs) 的 `thread_resume_params_from_config(...)` 明确写入 `Some(config.model_provider_id.clone())`；embedded TUI 侧 [app_server_session.rs](I:/vscodeProject/codex/codex-rs/tui/src/app_server_session.rs) 的 `ThreadParamsMode::Embedded` 同样从 `config.model_provider_id` 取当前 provider。
- 本次执行结果：F11 按新的两条 local1 口径复核通过；无需代码修改、无需 subagent、无需重新打 `release`。
